type ProductType = {
  cod: string;
  name: string;
  price: number;
  unit: string;
  size?: string;
  marca?: string;
  characteristic?: string;
  category: string;
  description?: string;
  image?: string;
};

export default ProductType;
